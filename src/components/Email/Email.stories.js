import { Email } from ".";

export default {
  title: "Components/Email",
  component: Email,
};

export const Default = {
  args: {
    className: {},
    divClassName: {},
    inputType: "email",
  },
};
